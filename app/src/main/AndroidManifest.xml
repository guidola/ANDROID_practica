<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="domel.ecampus">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/la_salle_logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Activity.WelcomeActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activity.LoginActivity"
            android:theme="@style/AppTheme.NoActionBar">
            android:screenOrientation="portrait"
            <!--
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            -->
        </activity>
        <activity android:name=".Activity.SubjectActivity" />
        <activity android:name=".Activity.StudentActivity" />
        <activity android:name=".Activity.AddStudentActivity" />
        <activity android:name=".Activity.AddSubjectActivity"></activity>
        <activity android:name=".Activity.SubjectManagerActivity" />
        <activity android:name=".Activity.StudentManagerActivity" />
        <activity android:name=".Activity.ExamsListActivity" />
        <activity android:name=".Activity.ExamEditorActivity"></activity>
    </application>

</manifest>